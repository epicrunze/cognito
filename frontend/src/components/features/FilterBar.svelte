<!-- TODO: FilterBar component
  - Filter tasks using Vikunja expression syntax: done = false && priority >= 3
  - Chips for active filters (click to remove)
  - Dropdown builders for common filters: priority, label, due date, assignee
  - Free-text search (uses Vikunja `s` param, not `search`)
  - Sort controls: field + direction
  - URL-sync: filters reflected in query params for shareable links
-->
<p>TODO: FilterBar</p>
