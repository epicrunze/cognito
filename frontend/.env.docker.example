# Cognito Frontend Docker Environment Variables
# Copy this file to .env.docker and configure for your deployment

# API URL (baked into the build at build time)
VITE_PUBLIC_API_URL=https://api-cognito.epicrunze.com/api
