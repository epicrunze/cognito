# Cognito Backend - Docker Environment Configuration
# Copy this file to .env.docker and fill in your actual values

### Database
# DuckDB database path (container path)
DATABASE_URL=duckdb:///./data/journal.duckdb

### Authentication (Google OAuth)
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# JWT Configuration
# Generate a secure random string: openssl rand -hex 32
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_EXPIRY_HOURS=24

# User Authorization
# Only this email address will be allowed to authenticate
ALLOWED_EMAIL=your.email@gmail.com

### Application
# Frontend URL for CORS
# For local development: http://localhost:5173
# For production: https://cognito.epicrunze.com (or your domain)
FRONTEND_URL=http://localhost:5173

# Cookie Security
# Set to True for HTTPS (production), False for HTTP (local development)
COOKIE_SECURE=False

### LLM Integration
# Gemini API Key (optional)
# Get from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Ollama URL
# If running Ollama on cognito-network, use service name: http://ollama:11434
# If running on host machine, use: http://host.docker.internal:11434
OLLAMA_URL=http://localhost:11434
